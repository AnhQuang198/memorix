---
scope: Memorix MVP (E1-E6)
purpose: build-substrate
altitude: feature
updated: 2026-07-07T23:35
---

- (direction) Coaching path, spine-only, altitude=feature, scope=MVP E1-E6. Nguồn: PRD final + spec 14-phase + addendum. Nhiều decision đã ADOPTED.
- (decision) Modular Monolith + Hexagonal core (Scheduling/Review); module=bounded context. ADOPTED từ spec Phase 7.
- (decision) Module gọi nhau chỉ qua interface public + event bus; cấm import struct nội bộ module khác. ADOPTED.
- (decision) Domain/service không phụ thuộc Gin/infra (port & adapter; Gin handler chỉ bind→gọi service). ADOPTED.
- (decision) Grade nguyên tử + idempotent unique(card_id,client_review_id); update card + insert review_log trong 1 TX. ADOPTED.
- (decision) ReviewLog append-only = nguồn chân lý; FSRS state rebuild-được qua replay theo server-ts. ADOPTED.
- (decision) Scheduling server-authoritative: client chỉ gửi grade, server tính S/D/Due. ADOPTED.
- (decision) Entry (nội dung) tách Card (FSRS/user); Card ref entryId, không nhúng. curated owner_id NULL dùng chung. ADOPTED.
- (decision) FSRS qua port bọc go-fsrs, không tự viết toán; cho A/B thuật toán trên cùng log. ADOPTED.
- (decision) Fork1: read model (Progress) async eventual, nguồn chân lý=review_logs. MVP=fire-and-forget in-process + job reconcile rebuild daily_stats. Nâng transactional outbox khi Notification lên V1. Prevents: ghép Progress vào hot path + mất event gây drift.
- (event) A-H adopted + Fork1 resolved
- (decision) Fork2a: truy cập dữ liệu chéo module CHỈ qua service/port module chủ (không join bảng schema khác). Prevents: rò rỉ ranh giới, cản tách service.
- (decision) Fork2b: FK chỉ trong cùng schema; chéo module = ref logic (cột id, không FK vật lý). Toàn vẹn do app (check tồn tại khi tạo card). Queue read model denormalize field entry hoặc batch-load qua Vocabulary port — không join hot path. Prevents: ghép chặt DB chéo module.
- (decision) Auth: access JWT stateless 15m verify ở middleware; refresh rotation+reuse-detect trong Identity; principal xuống service không xuống domain. ADOPTED.
- (decision) Due so theo server time; ngày học + daily-limit đếm theo TZ user; không tin đồng hồ client (FR-18). ADOPTED.
- (decision) Migration golang-migrate versioned, expand→migrate→contract (2 version song song không vỡ). ADOPTED.
- (decision) Config 12-factor env; secrets SOPS/Vault prod; không commit secret. ADOPTED.
- (decision) API: envelope {error:{code,message,fields,trace_id}}; cursor pagination; /api/v1 path versioning. Convention.
- (decision) Naming: module=bounded context (identity/vocabulary/scheduling/review/progress/notification); event PascalCase quá khứ (CardGraded); table snake_case. Convention.
- (decision) Job runner River (Postgres-backed); enqueue cùng TX domain khi cần đảm bảo (nền outbox V1). ADOPTED.
- (decision) Operational MVP: Docker Compose 1 VPS; Caddy TLS; staging+prod; Postgres+Redis+S3; Prometheus/Grafana/Loki prod. ADOPTED.
- (version) Go 1.23 · Gin · pgx/sqlc · golang-migrate · River · Postgres 16 · Redis 7 · go-fsrs · React+TS+Vite (client)
- (question) OQ-2 ngưỡng chống nổ chính xác; OQ-3 N North Star=7; OQ-4 Apple OAuth timing — từ PRD, tinh chỉnh sau.
- (direction) DEFERRED: Outbox transactional cho event (V1 khi Notification lên)
- (direction) DEFERRED: OpenSearch (khi pg FTS ép)
- (direction) DEFERRED: Sync 2 chiều / CRDT (V1.5)
- (direction) DEFERRED: Shard / tách service theo bounded context (1M+)
- (direction) DEFERRED: FSRS optimizer weights/user (V2, cần ~1000 log)
- (direction) DEFERRED: RN/Expo mobile app (V1.5); PWA offline (V1.5)
- (event) Sweep adopted; bắt đầu distill spine
- (change) Reviewer gate PASS-WITH-FIXES → fix 6: sửa dependency diagram (bỏ repo→service), AD-8 thêm rule số tức thì đọc từ review_logs, [ADOPTED] cho AD-8..14, AD-11 thêm OAuth linking, deployment thêm PITR RPO<5m, AD-13 ghi chú stage.
- (event) spine finalized
- (event) close
- (decision) Structural mapping chi tiết ghi vào companion addendum-structure.md: cmd/internal/platform + by-module + hexagonal chọn lọc (lõi scheduling/review full, còn lại nhẹ); enforce ranh giới bằng internal/<module>/internal/ (compiler) + depguard (domain không import hạ tầng) + Wire. Governs AD-1/2/9/10.
